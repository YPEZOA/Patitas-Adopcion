import React from 'react'
import { headerLogo as St } from './styles'
import Icon from 'react-native-vector-icons/FontAwesome'

const HeaderLogo = () => {
  return <Icon style={St.icon} name="paw" size={30} />
}

export default HeaderLogo
