import { StyleSheet } from 'react-native'

export const loaderStyles = StyleSheet.create({
  loaderContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  lottieLoading: {
    width: 120,
    height: 120,
  },
})
