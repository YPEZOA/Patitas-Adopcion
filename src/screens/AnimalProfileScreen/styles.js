import { StyleSheet } from 'react-native'

export const animalStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    flexDirection: 'row',
  },
  image: {
    width: '98%',
    height: '50%',
    borderRadius: 53,
  },
})
